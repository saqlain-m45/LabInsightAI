# LabInsight AI - Database Directory

This directory is reserved for database schemas, SQL scripts, and migration files.

### Planned Contents for Future Stages:
- `schema.sql`: Table structure for patients, CBC lab test runs, rule evaluation logs, and AI explanations.
- `seed.sql`: Sample reference range tables and test patient datasets.
